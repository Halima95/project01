


<div class="row">
<% @categories.each do |c| %>
<div class="col s3">
<div class="card medium">
    <div class="card-content">
      <span class="card-title activator grey-text text-darken-4">
       <a class="color1", href="<%=category_path(c)%>"><%= c.name  %></a>
      </span>
    </div>
  </div>
 </div>
<% end %>
</div>

